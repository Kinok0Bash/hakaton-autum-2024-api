spring:
  application:
    name: Eureka-Server

eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka
    fetch-registry: false
    register-with-eureka: false

server:
  port: 8761
